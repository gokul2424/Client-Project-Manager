import { SortprojectPipe } from './sortproject.pipe';

describe('SortprojectPipe', () => {
  it('create an instance', () => {
    const pipe = new SortprojectPipe();
    expect(pipe).toBeTruthy();
  });
});
