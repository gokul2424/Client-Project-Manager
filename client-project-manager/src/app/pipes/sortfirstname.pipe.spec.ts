import { SortfirstnamePipe } from './sortfirstname.pipe';

describe('SortfirstnamePipe', () => {
  it('create an instance', () => {
    const pipe = new SortfirstnamePipe();
    expect(pipe).toBeTruthy();
  });
});
